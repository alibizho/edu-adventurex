# Frontend — Teachable Student

**Owner: UI.** Not scaffolded yet — set it up however you like.

Planned stack (README): **React + Tailwind**, canvas sprite for the 8-bit student face.

The results screen is the demo — treat it as the first-class deliverable:
- live disturbance meter during teaching,
- the confidence × competence 2×2 (report §6),
- negative delta in red, and the exact sentence that lost each classmate.

Backend is live at `http://localhost:8000` — see `/docs`. Suggested first call: `POST /teach/turn`.
