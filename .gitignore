# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets  (.env.* also catches copies like .env.txt / .env.local)
.env
.env.*
!.env.example

# Node / frontend
node_modules/
dist/
.next/
.vite/
.vite-pid
*.tsbuildinfo

# Dev server logs
vite.*.log
uvicorn.*.log

# Data / models (keep dirs via .gitkeep, ignore contents)
data/*
!data/.gitkeep
*.wav
*.mp3
*.sqlite3

# OS / editor
.DS_Store
.idea/
.vscode/

# Junk / scratch
*.zip
/1.json
qa-*.png

# Screenshots and design references dropped in the repo root while working on the UI. Real
# assets live under frontend/public/, so nothing at the top level is ever worth committing.
/*.png
/*.jpg
/*.jpeg
