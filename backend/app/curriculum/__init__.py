"""Learning-plan (curriculum) layer: scope a topic, structure it into ~5 classes, and generate
Markdown teacher's notes per class. Teaching reuses the existing confusion/targeted-question
pipeline (see teaching.py) with cross-class memory."""
